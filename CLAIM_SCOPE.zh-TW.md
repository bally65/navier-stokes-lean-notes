# 宣稱範圍（中文版）

*這一頁說明這份工作宣稱了什麼，以及更重要的、它沒有宣稱什麼。請在讀任何其他文件之前先讀這一頁。
如果本倉庫其他文件看起來宣稱得比這一頁多，以這一頁為準，那份文件是錯的。*

## 一段話的版本

這是用 Lean 4 與 mathlib 對三維不可壓縮 Navier–Stokes 方程的局部正則性理論所做的形式化，依循
Lin (1998) 的量化部分與 Vasseur (2007) 的 De Giorgi 路線。它證明了該理論中的一個組成部分——一條
量化的二選一不等式——以及其底下的奇異積分機器。它**沒有**證明 ε-regularity，**沒有**證明部分正則性，
對全域存在性與光滑性隻字未提，與 Clay 千禧年問題無關。這裡所有東西都建立在一個公理化的解類別上，
而該類別目前唯一被展示出來的住民是零解。

## 已經證明的部分

以下每一項都經過機器檢查。凡是條件式的結果，其條件都是陳述裡具名的假設，不是藏在文字敘述中的默契。

**公理收據是部分覆蓋的，數字如下。** 近期的單位每條宣告後面都有 `#print axioms`，較舊的材料沒有。
在追蹤中的樹上量測：3456 個磚檔裡有 437 個完全沒有任何 `#print axioms`，宣告約 14931 條、
收據行約 9651 行。這一頁的前一版寫成「全部覆蓋」，那是錯的，而且一行指令就能推翻。

- **Lin 定理 3.3 在 ℝ³ 上的量化形式。** 對縮放量 `A`（速度能量）與 `D`（壓力），令
  `X = A^{3/2} + D²`，存在絕對常數 `θ₀ ≤ 1/8` 與有限常數 `C`，使得當縮放耗散在外層尺度夠小時，
  要嘛 `X` 在下一個尺度至少減半，要嘛 `X` 已經被 `C · B^{3/2}` 控制住。第二種形式接受 Lin 的
  `limsup` 假設，在某個 `r₀` 以下的每個半徑都給出這個二選一。
- **組成它的三條不等式**，都在 ℝ³ 上：Lin 的 (3.17)（取較好的指數）、中心化能量預算 (3.19)、
  中心化壓力遞迴 (3.20)。
- **ℝ³ 上的 Calderón–Zygmund 與 Riesz 層**：帶停止時間的二進方塊、分解、弱 (1,1) 界、雙重 Riesz
  算子、它在 `L^{3/2}` 上的有界性，以及乘子與齊次核的辨識。
- **Vasseur De Giorgi 路線的初等層**：他的層級集與遞縮柱體、迭代引理、Lemma 10 的逐點內容，
  以及把 `‖v‖` 的鏈規則歸約到正則化（外函數光滑）那一版。

## 沒有證明的部分，講得越白越好

- **沒有 ε-regularity。** 把縮放量的小性轉成速度的界，那一步在本倉庫任何地方都沒有嘗試。
- **沒有部分正則性。** 這裡沒有任何東西界定奇異點集的大小。特別是，沒有任何東西達到
  Caffarelli–Kohn–Nirenberg 的結論。
- **沒有全域存在性、沒有全域光滑性、沒有 Clay 結論。** Clay 問題問的是所有時間的光滑解。這份工作
  沒有處理它；就算做出部分正則性也一樣沒有處理它，因為測度為零的奇異點集不等於空集。
- **類別沒有存在性定理。** 解的類別是公理化定義的。本倉庫唯一展示出來的住民是零解。這裡沒有
  Leray–Hopf 存在性定理，所以每一條陳述都該讀成「對任何滿足這組公理的物件」，而不是
  「對已知存在的那些 Navier–Stokes 解」。
- **De Giorgi 路線沒有完成。** 它的核心義務——層級集能量不等式——沒有證明，甚至還沒有寫成陳述，
  因為要寫出來得先對壓力項做一個建模決定，而那個決定還沒做。它需要的鏈規則被當成具名的開放假設
  `CknVasseurAbsChainRule` 帶著走，已經歸約但還沒解決。

## 條件性，集中列在一處

| 項目 | 依賴於 |
|---|---|
| 定理 3.3 的二選一 | 公理化的解類別，包含它的局部能量不等式 |
| De Giorgi 的逐點層 | 同一個類別 |
| 鏈規則下游的任何東西 | `CknVasseurAbsChainRule`，一個開放假設 |

## 這份工作是怎麼產生的

本開發的絕大部分由 AI 代理在人類主導的戰役下寫成，每個單位在被接受之前都經過閘門：由寫它的代理
以外的人重新編譯、檢查有無非標準公理、並附上負向控制——把單一條陳述變異，要求證明因此壞掉。
每個單位的閘門數字都有記錄。這些控制檔目前不在公開的樹裡。

讀這個倉庫的時候請把這件事納入考慮。機器檢查確立的是「證明從陳述推得出來」；它完全沒有確立
「陳述是不是對的那一條」，而陳述正是人類判斷、也就是風險所在的地方。

## 對他人工作的獨立驗證

本倉庫另外含有一份對第三方公開形式化成果的稽核。該稽核只報告在本機上量測到的事：與它所沿用的
上游陳述做檔案層級比對、它的定理依賴哪些公理、以及它能不能建置。那不是對該工作的數學審查；
稽核過程中曾回報過一次建置失敗，後來追出是本機自身的並行建置所致，已經撤回並保留紀錄。

No epsilon regularity, no partial regularity, no global smoothness, and no Clay conclusion.
